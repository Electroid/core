name: CI

on:
    workflow_dispatch:
    workflow_call:
    pull_request:

permissions:
    checks: write
    contents: read

jobs:
    ci:
        name: Continuous Integration
        runs-on: ubuntu-latest
        defaults:
            run:
                shell: bash
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Bun
              uses: oven-sh/setup-bun@v1
              with:
                  bun-version: latest

            - name: Install dependencies
              run: bun install

            - name: Formatting
              run: bun run format:check

            - name: Build
              run: bun run build

            - name: Linting
              run: bun run lint

            - name: 'Unit tests: common'
              run: bun zenfs-test -pfw --common

            - name: 'Unit tests: InMemory'
              run: bun zenfs-test -pfw tests/setup/memory.ts

            - name: 'Unit tests: contexts'
              run: bun zenfs-test -pfw tests/setup/context.ts

            - name: 'Unit tests: Copy-on-Write'
              run: bun zenfs-test -pfw tests/setup/cow.ts

            - name: 'Unit tests: Index'
              run: bun zenfs-test -pfw tests/setup/index.ts

            - name: 'Unit tests: Port'
              run: bun zenfs-test -pfw tests/setup/port.ts

            - name: 'Unit tests: SingleBuffer'
              run: bun zenfs-test -pfw tests/setup/single-buffer.ts

            - name: 'Unit tests: Fetch'
              run: tests/fetch/run.sh -w

            - name: Report coverage
              run: bun zenfs-test --report
